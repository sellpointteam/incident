# Copy to .env.local and fill values. Never commit real credentials.
# These are safe *publishable* keys — do NOT put service_role or DB passwords here.
VITE_SUPABASE_PROJECT_ID=
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
